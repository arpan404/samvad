# Protocol

Samvad uses a protocol-first design.

`@samvad/protocol` owns client frame types, server event types, shared constants, error codes, protocol versioning, and schema definitions. Runtime packages and adapters implement the protocol; they do not redefine it.

Protocol details are placeholders until the wire format is designed.
