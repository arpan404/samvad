# @samvad/protocol

Owns the public Samvad protocol: client frames, server events, error codes, versioning, schema definitions, and shared protocol constants.

This package must not import runtime-specific APIs, server code, database code, WebSocket libraries, Redis, Postgres, Node APIs, Bun APIs, or Cloudflare APIs.
