# @samvad/client

Owns the browser/runtime-neutral client SDK: connection management, reconnect behavior, message sending, event subscription, conversations, typing, presence, and read-state helpers.

This package depends on `@samvad/protocol` and does not depend on React.
