# @samvad/server

Owns the runtime-neutral server layer: HTTP route definitions, WebSocket frame routing, request validation, auth boundaries, connection lifecycle abstractions, and server composition.

Runtime-specific code belongs in adapters, not here.
